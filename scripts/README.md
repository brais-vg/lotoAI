# Scripts

Utilidades para desarrollo y operaciones (bootstrap de datos, smoke tests, validaciones CI).
Mantenerlos idempotentes y compatibles con entornos locales/CI.

Comandos rápidos:
- `./scripts/init.sh` o `./scripts/init.ps1`: build + up de perfiles core y app.
- `./scripts/start.sh` o `./scripts/start.ps1`: up de perfiles core y app (sin build).
- `./scripts/stop.sh` o `./scripts/stop.ps1`: down de perfiles core y app.
