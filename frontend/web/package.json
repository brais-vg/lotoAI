{
  "name": "lotoai-web",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "serve -s . -l 3000",
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
